# Path to your list file (one item per line)
LIST_FILE="/path/to/your/items.txt"

# Script to run — receives the current item as $1
PROCESS_SCRIPT="/path/to/your/process.sh"

# Optional: custom lock file location
# LOCK_FILE="/tmp/my_chain.lock"
